# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PromisePlusRealm' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  swift_version = "3.0"
  pod "PromiseKit", "~> 4.0" 
  pod 'Bluebird', '~> 1.10'
  pod 'SnapKit', '~> 3.2.0'
  pod 'BrightFutures'  
  pod 'RealmSwift'
  # Pods for PromisePlusRealm

end
